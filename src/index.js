import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <h1 className="text-3xl text-yellow-500 bg-purple-500  font-bold underline">
      Hello world!
    </h1>
  </React.StrictMode>
);


